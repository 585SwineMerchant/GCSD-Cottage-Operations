window.GCSD_CONFIG = Object.freeze({
  // Use only the two /exec URLs created for GCSD Cottage Operations version two.
  publicFeedUrl: "https://script.google.com/macros/s/PUBLIC_FEED_DEPLOYMENT_ID/exec",
  teacherCommandCenterUrl: "https://script.google.com/macros/s/TEACHER_DEPLOYMENT_ID/exec"
});
